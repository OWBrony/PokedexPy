import pygame

class scroller:
    def __init__(self,list,name):
        pygame_menu.widgets.Scrollbar(length(len(list)),values_range((0,10),list),
        scrollbar_id(name), orientation(ORIENTATION_VERTICAL))
        # # length needs (int)
        # self._length = 0
        # # Values_range needs (tuple, list)
        # self._values_range = None
        # # Scrollbar_id needs a string
        # self.scrollbar_id = ""
        # # orientation needs ORIENTATION_HORIZONTAL or
        # # ORIENTATION_VERTICAL as a string.
        # self.orientation = ""
        # # slider_pad needs (int,float)
        # self.slider_pad = ""
        # # slider_color needs (tuple, list)
        # self.slider_color = None
        # # page_ctrl_thick needs (int,float)
        # self.page_ctrl_thick = None
        # # page_ctrl_color needs (tuple,list)
        # self.page_ctrl_color = None
        # # onchange (callable, None)
        # # onreturn (callable, None)
        pass